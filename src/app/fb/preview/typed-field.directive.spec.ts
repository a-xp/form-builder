import { TypedFieldDirective } from './typed-field.directive';

describe('TypedFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new TypedFieldDirective();
    expect(directive).toBeTruthy();
  });
});
